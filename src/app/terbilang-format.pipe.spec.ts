import { CurrencyFormatPipe } from './terbilang-format.pipe';

describe('TerbilangFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TerbilangFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
